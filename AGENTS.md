# Agent instructions

Same content as `CLAUDE.md` - read that file. It tells you how to check the
Instagram session, which skill in `.claude/skills/` handles which request,
and the rules that never bend (exact counts, no invented metrics, no emojis,
never commit `.env` or `creator_intelligence.db`).
